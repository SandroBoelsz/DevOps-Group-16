apiVersion: v1
kind: Service
metadata:
  name: lifewatch-service
  labels:
    app: lifewatch-app 
spec:
  selector:
    app: lifewatch-app
  ports:
    - port: 8080
      targetPort: 8080

  type: LoadBalancer